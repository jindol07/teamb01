<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.teamb.dfsms.dao.ProductsDao">

	<!-- List<ProductsVO> selectProductList(); -->
	<select id="selectProductList" resultType="ProductsVO" parameterType="map"> 
		select 
			PRODUCTID
			,CATEGORYID
			,PNM
			,PRICE
			,imgnm
			,row_num
		FROM 
		(
			SELECT
				a.PRODUCTID
				,a.CATEGORYID
				,a.PNM
				,a.PRICE
				,b.IMGNM
				,ROW_NUMBER() OVER(ORDER BY a.PRODUCTID desc) AS row_num
			FROM PRODUCTS a 
				JOIN p_Gallery b 
					ON a.PRODUCTID = b.PRODUCTID
			<where> 
				a.delyn = 'N'
				<if test="searchType != null and searchValue != null">
					<choose>
						<when test="searchType == 1">
							and a.CATEGORYID = 1
							AND a.PNM like '%'|| #{searchValue} ||'%'
						</when>
						<when test="searchType == 2">
							and a.CATEGORYID = 2
							AND a.PNM like '%'|| #{searchValue} ||'%'
						</when>
						<when test="searchType == 3">
							and a.CATEGORYID = 3
							AND a.PNM like '%'|| #{searchValue} ||'%'
						</when>
						<when test="searchType == 4">
							and a.CATEGORYID = 4
							AND a.PNM like '%'|| #{searchValue} ||'%'
						</when>
						<when test="searchType == 5">
							and a.CATEGORYID = 5
							AND a.PNM like '%'|| #{searchValue} ||'%'
						</when>
					</choose>
				</if>
			</where>
		) products_numrow_view
		WHERE row_num BETWEEN #{begin} AND #{end}
	</select>
	
	<!-- int totalCount(Map<String, String> map); -->
	<select id="totalCount" resultType="int" parameterType="map">
		select count(*) cnt from PRODUCTS
		<where>
			delyn = 'N'
			<if test="searchType != null and searchValue != null">
				<choose>
					<when test="searchType == 1">
						AND a.CATEGORYID = 1
						AND a.PNM like '%'|| #{searchValue} ||'%'
					</when>
					<when test="searchType == 2">
						AND a.CATEGORYID = 2
						AND a.PNM like '%'|| #{searchValue} ||'%'
					</when>
					<when test="searchType == 3">
						AND a.CATEGORYID = 3
						AND a.PNM like '%'|| #{searchValue} ||'%'
					</when>
					<when test="searchType == 4">
						AND a.CATEGORYID = 4
						AND a.PNM like '%'|| #{searchValue} ||'%'
					</when>
					<when test="searchType == 5">
						AND a.CATEGORYID = 5
						AND a.PNM like '%'|| #{searchValue} ||'%'
					</when>
				</choose>
			</if>
		</where>
	</select>

	<!-- ProductsVO selectProductDetail(int productid); -->
	<select id="selectProductDetail" resultType="ProductsVO" parameterType="int">
		SELECT 
			a.PRODUCTID
			,a.CATEGORYID
			,a.PNM
			,a.PRICE
			,b.IMGNM
		FROM PRODUCTS a
			JOIN p_Gallery b
				ON a.PRODUCTID = b.PRODUCTID
		WHERE a.PRODUCTID = #{productid}
	</select>

	<insert id="addProduct" parameterType="ProductsVO">
		insert into products values(products_seq.NEXTVAL, #{categoryid}, #{pnm},
		#{price}, #{qty}
		,#{title}, #{cont}, 0, 'N', sysdate)
	</insert>

	<insert id="addImg" parameterType="java.util.List">
		<!-- 현재 시퀀스 값 가져오기 -->
		<selectKey keyProperty="id" resultType="int" order="BEFORE">
			SELECT products_seq.currVal FROM dual
		</selectKey>
		<foreach collection="list" item="image" separator=" " open="insert all"
			close="select * from dual">
			INTO p_gallery (productid, imgnm)
			VALUES(#{id}, #{image.imgnm})
		</foreach>
	</insert>
	
	<update id="productUpdate" parameterType="ProductsVO">
		update products set
		categoryid =#{categoryid},pnm = #{pnm},price = #{price},qty
		=#{qty},title =#{title},cont =#{cont}
		where productid = #{productid}
	</update>

	<update id="productsDel" parameterType="int">
		update products set delyn='Y' where productid = #{num}
	</update>

</mapper>